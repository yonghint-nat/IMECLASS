const menu=document.querySelector('.menu-btn');const nav=document.querySelector('.nav-links');if(menu){menu.addEventListener('click',()=>nav.classList.toggle('open'))}
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const search=document.querySelector('#docSearch');const filters=document.querySelectorAll('.filter');const cards=document.querySelectorAll('.doc-card');const empty=document.querySelector('.empty');let active='all';function filterDocs(){const q=(search?.value||'').toLowerCase();let n=0;cards.forEach(c=>{const show=(active==='all'||c.dataset.type===active)&&c.innerText.toLowerCase().includes(q);c.style.display=show?'grid':'none';if(show)n++});if(empty)empty.style.display=n?'none':'block'}filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.filter;filterDocs()}));search?.addEventListener('input',filterDocs);
